import { AndroidRobot, getConnectedDevices } from "../src/android"; async function main() { const devices = getConnectedDevices(); if (devices.length === 0) { console.error("No Android devices found"); return; } const android = new AndroidRobot(devices[0]); await android.adb("shell", "input", "keyevent", "KEYCODE_HOME"); await android.openUrl("https://www.example.com"); const elements = await android.getElementsOnScreen(); console.log("Found elements:", elements); } main().catch(console.error);
